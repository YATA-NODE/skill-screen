# local test environment — fixtures (incl. a fake credential) + the test harness.
# Kept local, never shipped: a downloaded security tool should not carry attack
# fixtures or a fake key. Run the suite locally with: bash test/test-scan.sh
test/

# scan output dumps
*.scan.json

# editor / OS
.DS_Store
*.swp

# secrets (defense in depth — this tool never needs them)
.env
.env.*
