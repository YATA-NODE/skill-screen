# The shipped-secret fixture intentionally contains a FAKE (non-real) AWS key so the
# secret-scan code path is exercised by the test corpus. Exclude it from the global
# hardcoded-secret hooks (write-time and pre-push) so it doesn't block commits/pushes.
# skill-screen's own scanner does NOT read this file, so it still detects the fixture.
corpus/malicious/shipped-secret/
